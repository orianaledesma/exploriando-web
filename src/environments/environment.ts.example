// Copiá este archivo como environment.ts y completá con tus keys locales.
// Este archivo SÍ está en git. environment.ts NO está en git.
export const environment = {
  production: false,
  emailjs: {
    serviceId:              '',  // EmailJS → Email Services → Service ID
    notificationTemplateId: '',  // EmailJS → Templates → template de notificación a Oriana
    confirmationTemplateId: '',  // EmailJS → Templates → template de confirmación al suscriptor
    publicKey:              '',  // EmailJS → Account → Public Key
  },
};
